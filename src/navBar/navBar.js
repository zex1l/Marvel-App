import './navBar.css';

const NavBar = () => { 
    return(
        <header>
            <div className="header__inner">
                <div className="logo">Weather</div>
                <nav className="nav">
                    <a href="# " className="nav_link">Call</a>
                    <a href="# " className="nav_link">email</a>
                    <a href="# " className="nav_link">instagram</a>
                </nav>
            </div>
            
            
        </header>
    )
}

export default NavBar;